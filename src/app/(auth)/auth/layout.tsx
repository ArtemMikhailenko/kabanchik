import { type PropsWithChildren } from 'react'

export default function AuthLayout({ children }: PropsWithChildren) {
  // TODO: if already signed in, redirect to home
  // TODO: if not signed in, show the auth layout

  return children
}
